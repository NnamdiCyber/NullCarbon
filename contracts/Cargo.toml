[workspace]
members = [
    "retirement_verifier",
    "nullifier_registry",
    "credit_registry",
]
resolver = "2"

[profile.release]
opt-level = "z"
overflow-checks = true
lto = true
debug = 0
strip = "symbols"
